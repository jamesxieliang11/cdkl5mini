# 希舞之家小程序 - Agent 文档

## 项目概述

**希舞之家**（xiwu-home）是一款面向 **CDKL5 缺陷症**（一种罕见儿童神经系统疾病）患者家庭的微信小程序。项目围绕 CDKL5 大会活动展开，同时提供日常健康记录管理功能，帮助病友家庭记录用药、发作、就诊等信息，并生成统计报告。

- **技术栈**：微信小程序原生开发 + 微信云开发（CloudBase）+ Vant Weapp UI 组件库
- **云环境 ID**：`cloud1-4g0dlvsdc0db6c89`
- **项目根目录**：`base/`

---

## 目录结构

```
base/
├── app.js                          # 应用入口，初始化云开发、用户登录、全局状态管理
├── app.json                        # 应用配置，页面路由、tabBar、全局组件注册
├── app.wxss                        # 全局样式
├── package.json                    # 依赖管理（@vant/weapp）
├── components/
│   ├── custom-tabbar/              # 自定义底部导航栏组件
│   └── ec-canvas/                  # ECharts 图表组件（用于报告页面）
├── pages/
│   ├── home/                       # 首页 - 健康概览仪表盘
│   ├── add-record/                 # 添加记录入口页（类型选择）
│   ├── medication-record/          # 调药记录表单页
│   ├── seizure-record/             # 发作记录表单页
│   ├── other-record/               # 其他记录表单页
│   ├── my-records/                 # 我的记录列表页（查看/筛选/编辑）
│   ├── reports/                    # 统计报告页（趋势图表/分布分析）
│   ├── user-profile/               # 个人信息完善页（患儿信息/病历上传）
│   ├── monthly-report/              # 月度汇报页面（分步表单：用药/发作/里程碑）
│   ├── admin-monthly-stats/        # 管理员月度汇报统计看板
│   ├── admin/                      # 管理员面板（数据库初始化管理）
│   ├── appointment/                # 义诊预约页
│   ├── appointment-form/           # 义诊预约表单页
│   ├── expert-detail/              # 专家详情页
│   ├── queue-status/               # 排队状态页
│   ├── messages/                   # 消息通知页
│   ├── role-select/                # 角色选择页
│   ├── tools/                      # 工具页
│   ├── profile/                    # 旧版个人中心页
│   ├── user/                       # 旧版用户页
│   ├── edit-drug-adjustment/       # 编辑调药记录页
│   ├── cart/                       # 购物车页（Vant 示例遗留）
│   └── goods/                      # 商品页（Vant 示例遗留）
├── utils/
│   ├── database.js                 # 数据库操作工具类（云函数调用封装）
│   └── user.js                     # 用户信息管理工具类（登录/登出/授权）
├── cloudfunctions/
│   ├── getOpenId/                  # 获取用户 OpenID
│   ├── getUserProfile/             # 获取用户详细信息
│   ├── userLogin/                  # 用户登录/注册
│   ├── updateUserProfile/          # 更新患者详细信息（病友家庭）
│   ├── updateSimpleProfile/        # 更新简单信息（工作人员/科研人员）
│   ├── initDatabase/               # 初始化基础数据库（科室/专家/议程/资源）
│   ├── initRecordsDatabase/        # 初始化记录数据库
│   ├── medicationRecord/           # 调药记录 CRUD
│   ├── seizureRecord/              # 发作记录 CRUD
│   ├── otherRecord/                # 其他记录 CRUD
│   └── monthlyReport/              # 月度汇报 CRUD + 追踪药物管理
├── database/
│   ├── collections.json            # 数据库集合定义及索引
│   ├── init.sql                    # 基础表结构 SQL（参考设计）
│   ├── init-data.js                # 初始化数据脚本
│   ├── records-schema.sql          # 患者记录表结构 SQL（参考设计）
│   └── migration-add-images-to-seizure-records.sql  # 发作记录图片字段迁移
└── miniprogram_npm/                # npm 构建产物（Vant Weapp）
```

---

## 用户角色体系

应用支持三种用户角色，通过 `app.globalData.userRole` 管理：

| 角色 | 标识 | 说明 |
|------|------|------|
| **病友家庭** | `patient` | 默认角色。可使用健康记录、义诊预约、排队查询、紧急联系等功能 |
| **科研人员** | `researcher` | 可查看会议资料、专家通讯录、学术议程、学术交流 |
| **工作人员** | `staff` | 可使用签到管理、任务分配、数据统计、系统设置 |

首次进入小程序时自动以 `patient` 角色登录，首页根据角色动态展示不同功能区块。

---

## 核心业务模块

### 1. 健康记录管理（核心功能）

三类记录均支持**创建、编辑、查看、搜索、分页**，数据优先存储在云数据库，本地存储作为备用。

#### 1.1 调药记录（`medication_records`）
- **表单字段**：记录时间、体重(kg)、药物列表（药名/剂量/单位/服用时间）、副作用
- **药物快捷选择**：内置 20+ 种儿童癫痫常用药物（丙戊酸钠、左乙拉西坦、加奈索龙等），自动填充默认剂量和单位
- **副作用标签**：14 种常见副作用快捷选择（嗜睡、头晕、食欲不振等）
- **历史记录模板**：可复制最近 5 条记录快速填写
- **页面**：`pages/medication-record/index`
- **云函数**：`medicationRecord`（action: create/update/delete/get/list/search）

#### 1.2 发作记录（`seizure_records`）
- **表单字段**：发作时间、发作类型、持续时间(分钟)、诱发因素、症状表现、图片
- **发作类型**：强直阵挛发作、失神发作、肌阵挛发作、局灶性发作、复杂性发作、其他
- **图片上传**：最多 5 张，上传至云存储 `seizure-images/` 目录
- **页面**：`pages/seizure-record/index`
- **云函数**：`seizureRecord`（action: create/update/delete/get/list/search）

#### 1.3 其他记录（`other_records`）
- **表单字段**：记录时间、类别、内容、备注、图片
- **类别选项**：手术记录、门诊记录、住院记录、检查报告、化验结果、日常观察、饮食记录、睡眠记录、体温测量、血压监测、心情变化、特殊事件等 14 种
- **图片上传**：最多 9 张，上传至云存储 `other-record-images/` 目录
- **页面**：`pages/other-record/index`
- **云函数**：`otherRecord`（action: create/update/delete/get/list/search）

#### 1.4 月度汇报（`monthly_reports` + `tracked_medications`）
- **功能**：每月一次的批量快速记录，整合用药追踪、发作统计和发育里程碑打卡
- **分步表单**：Step 1 用药追踪 → Step 2 发作统计 → Step 3 发育里程碑
- **用药追踪**：重点追踪别嘌醇，支持配置追踪其他药物（剂量/频率/依从性/副作用/是否调整）
- **发作统计**：本月总发作次数、各类型发作次数、与上月对比、最严重发作描述、常见诱因
- **发育里程碑**：大运动/精细运动/语言认知/社交情感 四大类 22 项里程碑打卡
- **草稿保存**：支持分多次填写，自动保存草稿
- **月份切换**：可查看/编辑历史月份的汇报
- **预填充**：自动从上月汇报和追踪药物配置中预填充数据
- **月底提醒**：每月 25 号后弹窗提醒未提交的用户，首页显示提醒卡片
- **入口**：首页常用功能 + TabBar 添加记录弹窗
- **页面**：`pages/monthly-report/index`
- **云函数**：`monthlyReport`（action: create/update/get/list/getLastReport/getTrackedMedications/updateTrackedMedications/checkMonthSubmitted）
- **数据库集合**：`monthly_reports`（月度汇报数据）、`tracked_medications`（用户追踪药物配置）

### 2. 首页仪表盘（`pages/home/index`）

- 展示当前日期、通知栏
- **常用功能入口**：我的记录、月度汇报、我的报告、反馈意见
- **月度汇报提醒**：当月未提交时在首页顶部显示提醒卡片
- **今日用药**：展示当天或最近的调药记录
- **发作记录**：展示最近 25 条发作记录
- **其他记录**：展示最近 40 条其他记录
- 根据用户角色展示不同功能区（病友服务/科研服务/工作台）

### 3. 我的记录（`pages/my-records/index`）

- **分类筛选**：全部 / 调药 / 发作 / 其他
- **日期范围筛选**：默认最近 30 天
- **分页加载**：每页 20 条，支持上拉加载更多
- **记录详情弹窗**：点击记录查看详情，支持图片预览
- **编辑跳转**：从详情弹窗跳转到对应的编辑页面

### 4. 统计报告（`pages/reports/index`）

- **宝宝信息展示**：姓名、年龄、体重、病史（从用户档案获取）
- **时间范围选择**：7天/30天/3个月/6个月/1年
- **整体概览**：发作次数、用药记录数、月均发作、其他记录数
- **发作统计**：趋势图（按日/周/月/年）、类型分布、最近发作列表
- **用药统计**：趋势图、药物使用排行
- **其他统计**：趋势图、分类分布

### 5. 个人信息（`pages/user-profile/index`）

- **患儿信息**：姓名、生日、体重、家长姓名、联系电话、与患儿关系、病史、备注
- **病历文件上传**：支持拍照/相册/文档，最多 10 个文件，单文件不超过 20MB
- **文件管理**：预览（图片/文档）、删除
- **云存储路径**：`medical-files/`
- **云函数**：`updateUserProfile`（病友家庭）、`updateSimpleProfile`（工作人员/科研人员）

### 6. 管理员面板（`pages/admin/admin`）

- 查看基础数据库和记录数据库的初始化状态
- 一键初始化基础数据库（科室/专家/议程/配置/资源）
- **月度统计入口**：跳转到月度汇报统计看板

### 6.1 管理员月度汇报统计看板（`pages/admin-monthly-stats/index`）

- **提交概览**：总用户数、已提交数、草稿数、提交率（含进度条）
- **用药统计**：药物使用排行（带百分比柱状条）、依从性分布（好/一般/差）、剂量调整数
- **发作统计**：总发作次数、人均发作次数、与上月对比分布（减少/持平/增加）、各类型发作次数排行
- **发育里程碑达成率**：22 项里程碑的达成人数和百分比排行
- **用户提交详情**：可展开的用户列表，展示每个用户的药物种类、发作次数、里程碑数
- **月份切换**：支持查看最近 12 个月的统计数据
- **云函数**：`monthlyReport`（action: adminStats/adminDetail）
- 一键初始化记录数据库
- 刷新数据库状态

---

## 数据库设计

### 云数据库集合（14 个）

| 集合名 | 说明 | 核心字段 |
|--------|------|----------|
| `users` | 用户信息 | openid, userRole, patientInfo, medicalFiles, hasCompleteProfile |
| `departments` | 义诊科室 | name, description, location, available_slots, is_active |
| `experts` | 专家信息 | name, title, hospital, department, specialty, clinic_time |
| `schedules` | 会议议程 | title, speaker_name, start_time, end_time, location, category |
| `appointments` | 义诊预约 | user_id, department_id, patient_name, queue_number, status |
| `queues` | 排队状态 | appointment_id, department_id, queue_number, status |
| `messages` | 消息通知 | user_id, title, content, type, is_read |
| `favorites` | 用户收藏 | user_id, item_id, item_type |
| `system_configs` | 系统配置 | config_key, config_value, description |
| `resources` | 文件资源 | name, file_url, file_type, category, is_public |
| `medication_records` | 调药记录 | user_id, record_time, weight, medications[], side_effects |
| `seizure_records` | 发作记录 | user_id, record_time, seizure_type, duration, triggers, symptoms, images[] |
| `other_records` | 其他记录 | user_id, record_time, category, content, remark, images[] |
| `record_statistics` | 记录统计 | user_id, stat_date, medication_count, seizure_count |

### 数据权限模型

- 所有记录类集合通过 `user_id` 字段实现数据隔离
- 云函数中校验 `user_id` 确保用户只能操作自己的数据
- 基础数据（科室/专家/议程等）为公共只读数据

---

## 云函数清单

| 云函数名 | 功能 | 触发方式 |
|----------|------|----------|
| `getOpenId` | 获取用户 OpenID | 前端调用 |
| `getUserProfile` | 获取用户详细信息 | 前端调用 |
| `userLogin` | 用户登录/注册，自动创建或更新用户记录 | 应用启动时自动调用 |
| `updateUserProfile` | 更新病友家庭的患儿信息和病历文件 | 个人信息页提交 |
| `updateSimpleProfile` | 更新工作人员/科研人员的简单信息 | 个人信息页提交 |
| `initDatabase` | 初始化基础数据（科室/专家/议程/配置/资源） | 管理员面板手动触发 |
| `initRecordsDatabase` | 初始化记录相关数据库集合 | 管理员面板手动触发 |
| `medicationRecord` | 调药记录 CRUD（create/update/delete/get/list/search） | 前端调用 |
| `seizureRecord` | 发作记录 CRUD（create/update/delete/get/list/search） | 前端调用 |
| `otherRecord` | 其他记录 CRUD（create/update/delete/get/list/search） | 前端调用 |

---

## 工具函数

### `utils/database.js`

封装所有云函数调用，提供统一的 Promise 接口：

- **基础数据**：`initDatabase()`, `checkDatabaseStatus()`, `getDepartments()`, `getExperts()`, `getSchedules()`, `getSystemConfig()`, `getResources()`
- **记录数据库**：`initRecordsDatabase()`, `checkRecordsStatus()`
- **调药记录**：`createMedicationRecord()`, `updateMedicationRecord()`, `deleteMedicationRecord()`, `getMedicationRecord()`, `listMedicationRecords()`, `searchMedicationRecords()`
- **发作记录**：`createSeizureRecord()`, `updateSeizureRecord()`, `deleteSeizureRecord()`, `getSeizureRecord()`, `listSeizureRecords()`, `searchSeizureRecords()`
- **其他记录**：`createOtherRecord()`, `updateOtherRecord()`, `deleteOtherRecord()`, `getOtherRecord()`, `listOtherRecords()`, `searchOtherRecords()`

### `utils/user.js`

封装用户认证相关功能：

- `wxLogin(userRole, needUserInfo)` - 微信授权登录（调用 wx.login → userLogin 云函数）
- `userLogin(params)` - 调用登录云函数
- `getUserInfo()` - 从本地存储获取用户信息
- `isLoggedIn()` - 检查登录状态
- `logout()` - 退出登录
- `getOpenId()` - 获取 OpenID
- `getUserProfile()` - 获取用户详细信息（需用户授权）

---

## 自定义组件

### `custom-tabbar`

自定义底部导航栏，替代微信原生 tabBar：

- **Tab 项**：首页(home)、添加记录(add-record)、管理(admin)
- **添加记录**：点击弹出 ActionSheet 选择记录类型（调药/发作/其他），跳转到对应表单页
- **状态同步**：通过定时器（500ms）轮询当前页面路径，自动更新激活状态
- 各页面通过 `this.getTabBar().updateState()` 主动触发状态更新

### `ec-canvas`

ECharts 微信小程序版本，用于报告页面的图表渲染。

---

## 本地存储 Key

| Key | 说明 |
|-----|------|
| `userInfo` | 用户信息对象 |
| `openid` | 用户 OpenID |
| `userRole` | 用户角色 |
| `userId` | 用户数据库 ID |
| `logs` | 应用启动日志 |
| `favoriteSchedules` | 收藏的议程 ID 列表 |
| `lastCheckTime` | 最后检查消息时间戳 |
| `medicationRecords` | 调药记录本地备份（最近 10 条） |
| `epilepsyDiary_seizureRecords` | 发作记录本地备份 |
| `epilepsyDiary_otherRecords` | 其他记录本地备份 |
| `epilepsyDiary_drugRecords` | 旧版调药记录（add-record 页面使用） |

---

## 页面路由（TabBar 页面）

| 路径 | 说明 | TabBar |
|------|------|--------|
| `pages/home/index` | 首页 | ✅ 主页 |
| `pages/add-record/index` | 添加记录 | ✅ 添加记录 |
| `pages/my-records/index` | 我的记录 | ✅ 我的记录 |
| `pages/user-profile/index` | 个人中心 | ✅ 个人中心 |
| `pages/admin/admin` | 管理面板 | ✅ 个人中心（复用） |

---

## 开发注意事项

1. **云环境**：确保 `app.js` 中的 `env` 配置为正确的云环境 ID
2. **npm 构建**：修改依赖后需在微信开发者工具中执行「工具 → 构建 npm」
3. **数据库初始化**：首次部署需通过管理员面板初始化基础数据库和记录数据库
4. **云函数部署**：每个云函数需单独右键上传部署（「上传并部署：云端安装依赖」）
5. **数据降级策略**：所有数据读取优先走云函数，失败时自动降级到本地存储
6. **图片存储**：发作记录图片存储在 `seizure-images/`，其他记录图片在 `other-record-images/`，病历文件在 `medical-files/`
7. **遗留页面**：`cart/`、`goods/`、`profile/`、`user/` 为 Vant 示例或旧版页面，可清理
8. **主题色**：主色调 `#34BFA3`（绿色），导航栏背景色与之一致
