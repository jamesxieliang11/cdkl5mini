/* 用户信息完善页面样式 — 适老化优化 */
.container {
  padding: 24rpx;
  background-color: #f7f8fa;
  min-height: 100vh;
}

.header {
  text-align: center;
  margin-bottom: 40rpx;
  padding: 40rpx 0;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 20rpx;
  color: white;
}

/* 页面标题 — 适老化放大 */
.title {
  font-size: 52rpx;
  font-weight: bold;
  display: block;
  margin-bottom: 12rpx;
}

/* 副标题 — 适老化放大 */
.subtitle {
  font-size: 30rpx;
  opacity: 0.9;
}

/* 分组区块 — 适老化：增加间距 */
.section {
  margin-bottom: 48rpx;
}

.section-title {
  display: flex;
  align-items: center;
  margin-bottom: 20rpx;
  padding: 0 20rpx;
}

.section-title .van-icon {
  margin-right: 10rpx;
  color: #1989fa;
}

/* 分组标题文字 — 适老化放大 */
.section-title text {
  font-size: 34rpx;
  font-weight: bold;
  color: #323233;
}

/* 分组副标题 — 适老化放大 */
.section-subtitle {
  font-size: 26rpx;
  color: #969799;
  margin-left: auto;
}

.van-cell-group {
  border-radius: 16rpx;
  overflow: hidden;
  box-shadow: 0 2rpx 12rpx rgba(100, 101, 102, 0.08);
}

.file-upload {
  padding: 20rpx;
  text-align: center;
}

.file-list {
  background: white;
  border-radius: 16rpx;
  overflow: hidden;
  box-shadow: 0 2rpx 12rpx rgba(100, 101, 102, 0.08);
}

.file-item {
  display: flex;
  align-items: center;
  padding: 24rpx;
  border-bottom: 1rpx solid #ebedf0;
}

.file-item:last-child {
  border-bottom: none;
}

.file-info {
  display: flex;
  align-items: center;
  flex: 1;
}

.file-icon {
  margin-right: 20rpx;
  color: #1989fa;
  font-size: 40rpx;
}

.file-details {
  flex: 1;
}

/* 文件名 — 适老化放大 */
.file-name {
  display: block;
  font-size: 30rpx;
  color: #323233;
  margin-bottom: 8rpx;
  word-break: break-all;
}

.file-size {
  font-size: 26rpx;
  color: #969799;
}

.delete-icon {
  color: #ee0a24;
  font-size: 36rpx;
  padding: 10rpx;
}

/* 操作按钮区域 — 适老化：增大按钮高度和字号 */
.actions {
  margin-top: 60rpx;
  padding: 0 20rpx;
}

.actions .van-button {
  margin-bottom: 24rpx;
  border-radius: 50rpx;
  height: 96rpx;
  line-height: 96rpx;
  --button-large-height: 96rpx !important;
  --button-default-font-size: 34rpx !important;
}

.skip-btn {
  color: #969799 !important;
  border-color: #ebedf0 !important;
}

/* 表单样式优化 — 适老化：标签 30rpx，输入值 32rpx */
.van-field__label {
  color: #646566 !important;
  font-weight: 500 !important;
  font-size: 30rpx !important;
}

.van-field__control {
  color: #323233 !important;
  font-size: 32rpx !important;
}

.van-cell {
  padding: 28rpx 32rpx !important;
}

.van-cell__title {
  color: #646566 !important;
  font-weight: 500 !important;
  font-size: 30rpx !important;
}

.van-cell__value {
  color: #323233 !important;
  font-size: 32rpx !important;
}

/* 必填项标识 */
.van-field[required] .van-field__label::after {
  content: '*';
  color: #ee0a24;
  margin-left: 4rpx;
}

.van-cell[required] .van-cell__title::after {
  content: '*';
  color: #ee0a24;
  margin-left: 4rpx;
}